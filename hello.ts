let companyname:string="UST"
let compLocation:string="Trivandrum"
let totalLearners:number=30
let isCourseCompleted:boolean=false
let storedValue:null=null
let undefinedValue:undefined=undefined

console.log(`The company name is ${companyname},
    the location is ${compLocation}
    ,the totalLearners are ${totalLearners},
    course Completion ${isCourseCompleted},
    storedValue${storedValue} and the 
    undefined value is ${undefinedValue}
    `
)